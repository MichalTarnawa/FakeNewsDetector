<Window x:Class="FakeNewsDetector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="clr-namespace:MdXaml;assembly=MdXaml"
        xmlns:local="clr-namespace:FakeNewsDetector"
        xmlns:vm="clr-namespace:FakeNewsDetector.ViewModel"
        xmlns:helpers="clr-namespace:FakeNewsDetector.Helpers"
        mc:Ignorable="d"
        Title="System OSINT - Detektor Fake Newsów" 
        Height="768" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A">

    <Window.DataContext>
        <vm:MainWindow_VM />
    </Window.DataContext>

    <Window.Resources>
        <helpers:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

        <SolidColorBrush x:Key="AppBackground" Color="#0F172A"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#1E293B"/>
        <SolidColorBrush x:Key="BorderColor" Color="#334155"/>
        <SolidColorBrush x:Key="PrimaryAccent" Color="#3B82F6"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#94A3B8"/>
        <Geometry x:Key="IconSend">M22 2L11 13 M22 2l-7 20-4-9-9-4 20-7z</Geometry>
        <Geometry x:Key="IconBot">M12 2a2 2 0 0 1 2 2v2h.5A2.5 2.5 0 0 1 17 8.5V11h1v2h-1v5a2 2 0 0 1-2 2H9a2 2 0 0 1-2-2v-5H6v-2h1V8.5A2.5 2.5 0 0 1 9.5 6H10V4a2 2 0 0 1 2-2m-3 9v2h2v-2h-2m4 0v2h2v-2h-2z</Geometry>
        <Geometry x:Key="IconHourglass">M 18,22 H 6 V 16 L 10,12 6,8 V 2 H 18 V 8 L 14,12 18,16 Z M 16,16.5 L 12,12.5 8,16.5 V 20 H 16 Z M 12,11.5 L 16,7.5 V 4 H 8 V 7.5 Z</Geometry>

        <DropShadowEffect x:Key="SoftShadow" BlurRadius="15" ShadowDepth="4" Direction="270" Color="#000000" Opacity="0.4"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryAccent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style x:Key="GradientButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#3B82F6" Offset="0"/>
                                    <GradientStop Color="#2563EB" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bdr" Property="Background" Value="#1E293B"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid Background="{StaticResource AppBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource SurfaceColor}" Padding="30,20" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderColor}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Centrum Weryfikacji Informacji" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="System Wieloagentowy • Powered by LangChain &amp; Gradio" FontSize="13" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Gradio API URL:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold" FontSize="13"/>
                        <Border CornerRadius="6" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="{StaticResource AppBackground}" Padding="5,2">
                            <TextBox Text="{Binding ApiUrl, UpdateSourceTrigger=PropertyChanged}" 
                                     Width="250" 
                                     Style="{StaticResource ModernTextBox}" 
                                     VerticalContentAlignment="Center"
                                     ToolTip="Wklej tutaj wygenerowany link z Google Colab (np. https://abc.gradio.live)"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <ScrollViewer x:Name="ChatScrollViewer" Grid.Row="1" Margin="30,20" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ChatHistory}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,20">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsUser}" Value="False">
                                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" VerticalAlignment="Top" Margin="0,0,10,0">
                                    <Path Data="{StaticResource IconBot}" Fill="{StaticResource PrimaryAccent}" Width="18" Height="18" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>

                                <Border Grid.Column="1" Padding="15" MaxWidth="800">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="CornerRadius" Value="0,16,16,16"/>
                                            <Setter Property="Effect" Value="{StaticResource SoftShadow}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource PrimaryAccent}"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                    <Setter Property="CornerRadius" Value="16,0,16,16"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <md:MarkdownScrollViewer Markdown="{Binding Text}" 
                                                             Background="Transparent"
                                                             MarkdownStyleName="Standard"
                                                             VerticalScrollBarVisibility="Disabled">
                                        <md:MarkdownScrollViewer.Style>
                                            <Style TargetType="md:MarkdownScrollViewer">
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </md:MarkdownScrollViewer.Style>
                                    </md:MarkdownScrollViewer>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Border Grid.Row="2" Background="{StaticResource SurfaceColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="20" Margin="30,0,30,30" CornerRadius="16" Effect="{StaticResource SoftShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Zadaj pytanie, podaj link do grafiki lub wklej treść..." Foreground="{StaticResource TextSecondary}" FontSize="15" Margin="5,0,0,0" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=InputBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBox x:Name="InputBox" Style="{StaticResource ModernTextBox}" 
                                 Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" 
                                 MaxHeight="100" TextWrapping="Wrap"
                                 IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Return" Command="{Binding SendCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </Grid>

                    <Button Grid.Column="1" Style="{StaticResource GradientButton}" Width="50" Height="50" Margin="15,0,0,0" Command="{Binding SendCommand}">
                        <Grid>
                            <Path Data="{StaticResource IconSend}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="20" Height="20" Stretch="Uniform" Margin="2,2,0,0">
                                <Path.Style>
                                    <Style TargetType="Path">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Path.Style>
                            </Path>

                            <Path Data="{StaticResource IconHourglass}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="20" Height="20" Stretch="Uniform" RenderTransformOrigin="0.5,0.5">
                                <Path.Style>
                                    <Style TargetType="Path">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard Name="SpinStoryboard">
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                                                                             From="0" To="180" Duration="0:0:1.5" 
                                                                             RepeatBehavior="Forever" AutoReverse="False"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <StopStoryboard BeginStoryboardName="SpinStoryboard"/>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Path.Style>
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Path.RenderTransform>
                            </Path>
                        </Grid>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>